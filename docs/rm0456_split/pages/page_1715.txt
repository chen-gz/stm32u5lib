RM0456 Rev 6

RM0456

43.4

LCD-TFT display controller (LTDC)

LTDC programmable parameters
The LTDC controller provides flexible configurable parameters. It can be enabled or
disabled through the LTDC_GCR register.

LTDC global configuration parameters
Synchronous timings
The figure below presents the configurable timing parameters generated by the
synchronous timings generator block presented in the block diagram Figure 407. It
generates the horizontal and vertical synchronization timings panel signals, the pixel clock
and the data enable signals.
Figure 408. LTDC synchronous timings
Total width
HFP

HSYNC
width

HBP
Active width

VSYNC width
VBP
Data1, Line1
Total height

43.4.1

Active display area
Active height

Data(n), Line(n)
VFP
Note: HBP and HFP are respectively the horizontal back porch and front porch period.
VBP and VFP are respectively the vertical back porch and front porch period.

MSv19674V2

The LTDC programmable synchronous timings are the following:
•

HSYNC and VSYNC width: horizontal and vertical synchronization width, configured by
programming a value of HSYNC width - 1 and VSYNC width - 1 in the LTDC_SSCR
register

•

HBP and VBP: horizontal and vertical synchronization back porch width, configured by
programming the accumulated value HSYNC width + HBP - 1 and the accumulated
value VSYNC width + VBP - 1 in the LTDC_BPCR register.

•

Active width and active height: the active width and active height are configured by
programming the accumulated value HSYNC width + HBP + active width - 1 and the

RM0456 Rev 6